sudo: required
dist: xenial
language: minimal

services:
- docker

git:
  submodules: false

before_install:
- travis_wait git submodule update --init --recursive

install:
- docker build .

script:
- bash quick-start.sh

branches:
  only:
  - master
  - wip
